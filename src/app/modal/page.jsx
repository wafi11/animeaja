import React from "react";
import Modal from "@/components/utilities/Modal";

const page = () => {
  //   const { fetchUser, data } = await useApiStore((state) => state);
  //   console.log(fetchUser);
  return (
    <div>
      page
      <Modal />
    </div>
  );
};

export default page;
